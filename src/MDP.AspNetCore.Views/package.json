{
  "devDependencies": {
    "cssnano": "^6.0.3",
    "grunt": "^1.6.1",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-postcss": "^10.0.0",
    "gulp-uglify": "^3.0.2",
    "postcss-import": "^16.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    
  },
  "scripts": {
    "publish": "npm install && gulp publish"
  }
}
